#name: Update Datasets
#
#on:
#  schedule:
#    - cron: "0 9 1 * *" # 1st of each month at 9am UTC
#  workflow_dispatch:
#
#jobs:
#  update-courts:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#
#      - uses: actions/setup-python@v5
#        with:
#          python-version: "3.12"
#
#      - run: pip install pymupdf
#
#      - name: Build courts database
#        run: python scripts/build_courts_database.py /tmp/va-dist-courts.pdf static/va-courts.json
#
#      - name: Commit if changed
#        run: |
#          git diff --quiet static/va-courts.json && exit 0
#          git config user.name "github-actions[bot]"
#          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
#          git add static/va-courts.json
#          git commit -m "Update Virginia courts directory"
#          git push
