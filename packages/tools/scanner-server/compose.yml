services:
  fake-scanner:
    build: .
    ports:
      - "8085:8085"
    volumes:
      - ./inbox:/watch
    environment:
      PORT: 8085
      WATCH_DIR: /watch
      POLL_MS: 500
      DELAY_MS: 1500
