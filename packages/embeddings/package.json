{
  "name": "@proseva/embeddings",
  "version": "0.0.0-prerelease",
  "description": "Builds knowledge graphs by precomputing embeddings",
  "scripts": {
    "dev": "cargo run --release --bin embedding-server",
    "build": "cargo build --release",
    "postinstall": "cargo check"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.52"
  }
}
